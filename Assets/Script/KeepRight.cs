using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class KeepRight : MonoBehaviour {
	/*
	public GameObject facingDirection;
	private Transform _direction;
	// Use this for initialization
	void Start () {
		_direction = facingDirection.GetComponent<Transform>.localScale.x;
	}
	
	// Update is called once per frame
	void Update () {
        this.transform.localScale = new Vector3(_direction, transform.localScale.y, 1);
	}
	*/
}
